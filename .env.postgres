# PostgreSQL Database Configuration for CV System
# NOTE: Add these to your main .env file when ready

# Database Connection
DATABASE_URL="postgresql://postgres:password@localhost:5432/sportsplatform_cv?schema=public"

# Alternative configurations:
# - For Docker PostgreSQL:
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/sportsplatform_cv?schema=public"

# - For Local PostgreSQL:
# DATABASE_URL="postgresql://postgres:your_password@localhost:5432/sportsplatform_cv?schema=public"

# - For Production (update with real credentials):
# DATABASE_URL="postgresql://user:password@db.server.com:5432/sportsplatform_cv?schema=public"

# Database Setup Instructions:
# 1. Install PostgreSQL locally or use Docker
# 2. Create database: createdb sportsplatform_cv
# 3. Create user: CREATE USER sportuser WITH PASSWORD 'your_password';
# 4. Grant privileges: ALTER ROLE sportuser CREATEDB;
# 5. Update DATABASE_URL above
# 6. Run: npx prisma migrate dev --name init
