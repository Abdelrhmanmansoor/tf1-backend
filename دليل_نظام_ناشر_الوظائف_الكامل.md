# 📖 دليل نظام ناشر الوظائف الكامل والاحترافي

## 📋 جدول المحتويات
1. [مقدمة](#مقدمة)
2. [البروفايل الاحترافي](#البروفايل-الاحترافي)
3. [نظام المحادثات](#نظام-المحادثات)
4. [نظام الإشعارات](#نظام-الإشعارات)
5. [إدارة الوظائف](#إدارة-الوظائف)
6. [إدارة الطلبات](#إدارة-الطلبات)
7. [العنوان الوطني](#العنوان-الوطني)
8. [التكامل مع الفرونت إند](#التكامل-مع-الفرونت-إند)

---

## 🎯 مقدمة

تم تطوير نظام شامل ومتكامل لناشري الوظائف يشمل:
- ✅ بروفايل احترافي مبني على أفضل الممارسات العالمية
- ✅ نظام محادثات متطور مع الباحثين عن وظائف
- ✅ نظام إشعارات ذكي ومربوط بجميع الأحداث
- ✅ إدارة كاملة للوظائف والطلبات
- ✅ توثيق العنوان الوطني السعودي
- ✅ رفع المستندات والصور
- ✅ إحصائيات دقيقة وشاملة

---

## 🏢 البروفايل الاحترافي

### معلومات الشركة الأساسية

#### 1. البيانات العامة
```javascript
{
  companyName: String,           // اسم الشركة بالإنجليزية
  companyNameAr: String,         // اسم الشركة بالعربية
  companyLogo: String,           // رابط شعار الشركة
  industryType: String,          // نوع الصناعة (16 خيار)
  companySize: String,           // حجم الشركة (5 فئات)
  websiteUrl: String             // موقع الشركة الإلكتروني
}
```

**أنواع الصناعات المتاحة**:
- Technology (تقنية)
- Finance (مالية)
- Healthcare (رعاية صحية)
- Retail (تجزئة)
- Manufacturing (تصنيع)
- Education (تعليم)
- Hospitality (ضيافة)
- Sports (رياضة)
- Construction (إنشاءات)
- Logistics (لوجستيات)
- Consulting (استشارات)
- Media (إعلام)
- Real Estate (عقارات)
- Energy (طاقة)
- Telecommunications (اتصالات)
- Other (أخرى)

**فئات حجم الشركة**:
- `1-10` موظفين
- `11-50` موظف
- `51-200` موظف
- `201-500` موظف
- `500+` موظف

#### 2. وصف الشركة
```javascript
{
  companyDescription: String,  // 50-2000 حرف
  companyValues: [String],     // قيم الشركة
  companyBenefits: [String]    // مزايا العمل في الشركة
}
```

**أمثلة على المزايا**:
- تأمين صحي شامل
- العمل عن بعد
- ساعات عمل مرنة
- برامج تدريبية
- مكافآت سنوية
- إجازات مدفوعة
- بيئة عمل محفزة

#### 3. بيئة العمل
```javascript
{
  workEnvironmentPhotos: [{
    url: String,           // رابط الصورة
    caption: String,       // وصف الصورة
    uploadDate: Date       // تاريخ الرفع
  }],
  companyVideoUrl: String  // فيديو تعريفي
}
```

**نصائح للصور**:
- استخدم صور عالية الجودة (1920x1080 أو أكثر)
- اعرض مكاتب العمل، غرف الاجتماعات، مناطق الاستراحة
- أظهر الفريق في بيئة العمل
- تجنب الصور المخزنة (stock photos)

#### 4. الجوائز والشهادات
```javascript
{
  awards: [{
    title: String,           // اسم الجائزة/الشهادة
    description: String,     // الوصف
    issuer: String,          // الجهة المانحة
    date: Date,              // تاريخ الحصول عليها
    certificateUrl: String   // رابط الشهادة
  }]
}
```

**أمثلة**:
- ISO 9001:2015
- أفضل بيئة عمل 2025
- شهادة الجودة من وزارة الموارد البشرية
- جائزة الابتكار

#### 5. آراء الموظفين
```javascript
{
  employeeTestimonials: [{
    employeeName: String,    // اسم الموظف
    position: String,        // المسمى الوظيفي
    testimonial: String,     // الرأي
    date: Date,              // التاريخ
    verified: Boolean        // تم التحقق؟
  }]
}
```

**نموذج رأي موظف**:
```
"بيئة العمل في هذه الشركة رائعة. الإدارة داعمة والفريق متعاون. 
أشعر بالتقدير والنمو المستمر في مهاراتي."
- أحمد محمد، مطور برمجيات
```

### معلومات التوظيف

#### 6. عملية التوظيف
```javascript
{
  hiringProcess: {
    averageTimeToHire: Number,    // متوسط أيام التوظيف
    processSteps: [String],        // خطوات العملية
    description: String            // وصف تفصيلي
  }
}
```

**مثال على الخطوات**:
1. مراجعة الطلب
2. مقابلة هاتفية
3. اختبار تقني
4. مقابلة شخصية
5. تقديم العرض

#### 7. المواقع والفروع
```javascript
{
  officeLocations: [{
    address: String,           // العنوان الكامل
    city: String,              // المدينة
    country: String,           // الدولة
    isPrimary: Boolean,        // المقر الرئيسي؟
    coordinates: {
      latitude: Number,
      longitude: Number
    }
  }]
}
```

### المعلومات القانونية

#### 8. السجلات والرخص
```javascript
{
  businessRegistrationNumber: String,  // رقم السجل التجاري (إلزامي)
  taxNumber: String,                   // الرقم الضريبي
  documents: {
    businessLicense: {
      url: String,           // رابط رخصة العمل
      uploadDate: Date,      // تاريخ الرفع
      verified: Boolean      // تم التحقق؟
    },
    taxCertificate: {
      url: String,           // رابط الشهادة الضريبية
      uploadDate: Date,
      verified: Boolean
    },
    nationalAddressDocument: {
      url: String,           // رابط مستند العنوان الوطني
      uploadDate: Date,
      verified: Boolean
    }
  }
}
```

#### 9. العنوان الوطني (السعودية)
```javascript
{
  nationalAddress: {
    buildingNumber: String,        // رقم المبنى (4 أرقام)
    additionalNumber: String,      // الرقم الإضافي (4 أرقام)
    zipCode: String,               // الرمز البريدي (5 أرقام)
    district: String,              // الحي
    city: String,                  // المدينة
    verified: Boolean,             // تم التحقق؟
    verificationDate: Date,        // تاريخ التحقق
    verificationAttempted: Boolean,// تم المحاولة؟
    verificationError: String      // خطأ التحقق (إن وجد)
  }
}
```

**مثال على عنوان وطني**:
```
رقم المبنى: 7240
الرقم الإضافي: 8228
الرمز البريدي: 12345
الحي: النرجس
المدينة: الرياض
```

### معلومات الاتصال

#### 10. بيانات الممثل
```javascript
{
  representativeName: String,      // اسم الممثل
  representativeTitle: String,     // المسمى الوظيفي
  representativePhone: String,     // رقم الهاتف
  representativeEmail: String,     // البريد الإلكتروني
  preferredContactMethod: String   // email, phone, both
}
```

**المسميات المتاحة للممثل**:
- CEO (المدير التنفيذي)
- HR Manager (مدير الموارد البشرية)
- Hiring Manager (مدير التوظيف)
- Founder (المؤسس)
- Director (مدير)
- Manager (مشرف)
- Owner (مالك)
- Other (أخرى)

#### 11. روابط السوشيال ميديا
```javascript
{
  socialMediaLinks: {
    linkedin: String,    // صفحة LinkedIn
    twitter: String,     // حساب X (Twitter)
    facebook: String,    // صفحة Facebook
    instagram: String,   // حساب Instagram
    youtube: String,     // قناة YouTube
    website: String      // الموقع الرسمي
  }
}
```

### المعلومات البنكية (اختياري)

#### 12. الحساب البنكي
```javascript
{
  bankAccountName: String,   // اسم صاحب الحساب
  bankName: String,          // اسم البنك
  bankIban: String,          // رقم الآيبان
  bankVerified: Boolean      // تم التحقق؟
}
```

### الإحصائيات

#### 13. بيانات الأداء
```javascript
{
  statistics: {
    totalJobsPosted: Number,          // إجمالي الوظائف المنشورة
    totalApplicationsReceived: Number, // إجمالي الطلبات المستلمة
    totalHired: Number,               // إجمالي التوظيفات
    profileViews: Number,             // عدد المشاهدات
    hireRate: Number                  // معدل التوظيف (%)
  },
  ratings: {
    average: Number,    // متوسط التقييم (0-5)
    count: Number       // عدد التقييمات
  }
}
```

---

## 🔗 API Endpoints للبروفايل

### 1. إنشاء بروفايل
```http
POST /api/v1/job-publisher/profile/create
Authorization: Bearer {token}
Content-Type: application/json

{
  "companyName": "شركة التقنية المتقدمة",
  "industryType": "technology",
  "companySize": "51-200",
  "businessRegistrationNumber": "1234567890",
  "representativeName": "أحمد محمد",
  "representativeTitle": "hr_manager",
  "representativePhone": "+966501234567",
  "representativeEmail": "hr@company.com",
  "companyDescription": "شركة رائدة في مجال التقنية..."
}
```

### 2. جلب البروفايل
```http
GET /api/v1/job-publisher/profile
Authorization: Bearer {token}
```

### 3. تحديث البروفايل
```http
PUT /api/v1/job-publisher/profile
Authorization: Bearer {token}
Content-Type: application/json

{
  "companyDescription": "وصف جديد...",
  "companyBenefits": ["تأمين صحي", "عمل عن بعد"],
  "companyValues": ["الابتكار", "الشفافية"]
}
```

### 4. رفع شعار الشركة
```http
POST /api/v1/job-publisher/profile/upload-logo
Authorization: Bearer {token}
Content-Type: multipart/form-data

logo: [FILE]
```

### 5. رفع صور بيئة العمل
```http
POST /api/v1/job-publisher/profile/upload-work-photo
Authorization: Bearer {token}
Content-Type: multipart/form-data

photo: [FILE]
caption: "غرفة الاجتماعات الرئيسية"
```

### 6. رفع المستندات
```http
POST /api/v1/job-publisher/profile/upload-document
Authorization: Bearer {token}
Content-Type: multipart/form-data

document: [FILE]
documentType: "businessLicense"
# أو "taxCertificate" أو "nationalAddressDocument"
```

### 7. إضافة جائزة
```http
POST /api/v1/job-publisher/profile/add-award
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "ISO 9001:2015",
  "description": "شهادة الجودة الدولية",
  "issuer": "المنظمة الدولية للمعايير",
  "date": "2024-01-15",
  "certificateUrl": "https://..."
}
```

### 8. إضافة رأي موظف
```http
POST /api/v1/job-publisher/profile/add-testimonial
Authorization: Bearer {token}
Content-Type: application/json

{
  "employeeName": "أحمد محمد",
  "position": "مطور برمجيات",
  "testimonial": "بيئة عمل رائعة..."
}
```

### 9. التحقق من العنوان الوطني
```http
POST /api/v1/job-publisher/profile/verify-national-address
Authorization: Bearer {token}
```

---

## 💬 نظام المحادثات

### كيف يعمل؟

1. **عند تغيير حالة الطلب إلى "interviewed"**:
   - يتم إنشاء محادثة تلقائياً
   - يتم إرسال إشعار للباحث عن وظيفة
   - تفتح قناة اتصال مباشرة

2. **المحادثات متاحة في**:
   - تبويب "الرسائل" في لوحة التحكم
   - صفحة تفاصيل الطلب
   - قائمة الإشعارات

### API Endpoints

#### 1. جلب جميع المحادثات
```http
GET /api/v1/messages/conversations
Authorization: Bearer {token}
```

**الرد**:
```json
{
  "success": true,
  "results": 5,
  "conversations": [
    {
      "_id": "conv123",
      "applicationId": "app456",
      "jobId": {
        "title": "مطور واجهات أمامية"
      },
      "participants": ["user1", "user2"],
      "subject": "مقابلة - مطور واجهات أمامية",
      "lastMessage": {
        "content": "شكراً على الرد...",
        "sentAt": "2026-01-09T10:00:00Z",
        "senderId": "user2"
      },
      "unreadCount": {
        "user1": 2,
        "user2": 0
      },
      "status": "active"
    }
  ]
}
```

#### 2. جلب محادثة محددة
```http
GET /api/v1/messages/conversations/:conversationId
Authorization: Bearer {token}
```

#### 3. جلب رسائل محادثة
```http
GET /api/v1/messages/:conversationId
Authorization: Bearer {token}
```

**الرد**:
```json
{
  "success": true,
  "messages": [
    {
      "_id": "msg123",
      "conversationId": "conv456",
      "senderId": {
        "_id": "user1",
        "firstName": "أحمد",
        "lastName": "محمد"
      },
      "content": "مرحباً، نود دعوتك للمقابلة يوم الأحد",
      "messageType": "text",
      "readBy": ["user1"],
      "createdAt": "2026-01-09T09:00:00Z"
    }
  ]
}
```

#### 4. إرسال رسالة
```http
POST /api/v1/messages/send
Authorization: Bearer {token}
Content-Type: application/json

{
  "conversationId": "conv456",
  "content": "شكراً، سأكون متواجداً"
}
```

#### 5. تحديث حالة الطلب مع رسالة
```http
PUT /api/v1/job-publisher/applications/:applicationId/status
Authorization: Bearer {token}
Content-Type: application/json

{
  "status": "interviewed",
  "message": "نود دعوتك لمقابلة يوم الأحد الساعة 10 صباحاً في مقر الشركة"
}
```

**ماذا يحدث؟**:
1. تتغير حالة الطلب إلى "interviewed"
2. يتم إنشاء محادثة (إن لم تكن موجودة)
3. يتم إرسال الرسالة في المحادثة
4. يتم إرسال إشعار للباحث عن وظيفة
5. يزداد عداد الرسائل غير المقروءة

---

## 🔔 نظام الإشعارات

### أنواع الإشعارات

#### 1. إشعارات حالة الطلب
- `application_new` - تم استقبال طلب جديد
- `application_under_review` - جاري مراجعة الطلب
- `application_interviewed` - تم اختيارك للمقابلة
- `application_offered` - تم تقديم عرض وظيفي
- `application_accepted` - تم قبول العرض
- `application_rejected` - لم يتم قبول الطلب
- `application_hired` - تم التوظيف بنجاح

#### 2. إشعارات الرسائل
- `message_received` - رسالة جديدة
- `message_reply` - رد على رسالتك
- `conversation_started` - بدأت محادثة جديدة

#### 3. إشعارات الوظائف
- `job_posted` - تم نشر وظيفة جديدة
- `job_application` - تقدم شخص للوظيفة
- `profile_viewed` - شاهد أحدهم بروفايلك

### مستويات الأولوية

- **urgent** (عاجل): أحمر - يحتاج إجراء فوري
- **high** (عالي): برتقالي - مهم جداً
- **normal** (عادي): أبيض - معلومات عادية

### API Endpoints

#### 1. جلب جميع الإشعارات
```http
GET /api/v1/notifications
Authorization: Bearer {token}

# فلترة
GET /api/v1/notifications?unread=true
GET /api/v1/notifications?type=application_interviewed
GET /api/v1/notifications?priority=high
```

**الرد**:
```json
{
  "success": true,
  "results": 10,
  "notifications": [
    {
      "_id": "notif123",
      "userId": "user456",
      "type": "application_interviewed",
      "title": "تم اختيارك للمقابلة",
      "message": "تم اختيارك لمقابلة وظيفة مطور واجهات أمامية",
      "priority": "normal",
      "read": false,
      "entityType": "application",
      "entityId": "app789",
      "metadata": {
        "jobTitle": "مطور واجهات أمامية",
        "companyName": "شركة التقنية",
        "applicationStatus": "interviewed"
      },
      "createdAt": "2026-01-09T09:00:00Z"
    }
  ]
}
```

#### 2. تحديد كمقروء
```http
PUT /api/v1/notifications/:notificationId/read
Authorization: Bearer {token}
```

#### 3. تحديد الكل كمقروء
```http
PUT /api/v1/notifications/read-all
Authorization: Bearer {token}
```

#### 4. حذف إشعار
```http
DELETE /api/v1/notifications/:notificationId
Authorization: Bearer {token}
```

---

## 📝 إدارة الوظائف

### إنشاء وظيفة جديدة
```http
POST /api/v1/jobs
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "مطور واجهات أمامية",
  "description": "نبحث عن مطور محترف...",
  "requirements": [
    "خبرة 3 سنوات في React",
    "إتقان TypeScript",
    "معرفة بـ Next.js"
  ],
  "responsibilities": [
    "تطوير واجهات المستخدم",
    "التعاون مع فريق التصميم",
    "كتابة كود نظيف"
  ],
  "location": "location_id_here",
  "employmentType": "full-time",
  "experienceLevel": "mid",
  "salaryRange": {
    "min": 8000,
    "max": 12000,
    "currency": "SAR"
  },
  "benefits": [
    "تأمين صحي",
    "عمل عن بعد",
    "مكافآت سنوية"
  ],
  "applicationDeadline": "2026-02-28",
  "status": "active"
}
```

### جلب وظائفي
```http
GET /api/v1/jobs/my-jobs
Authorization: Bearer {token}
```

### تحديث وظيفة
```http
PUT /api/v1/jobs/:jobId
Authorization: Bearer {token}
```

### إغلاق وظيفة
```http
PUT /api/v1/jobs/:jobId
Authorization: Bearer {token}

{
  "status": "closed"
}
```

---

## 📊 إدارة الطلبات

### جلب جميع الطلبات
```http
GET /api/v1/job-publisher/applications
Authorization: Bearer {token}

# فلترة
GET /api/v1/job-publisher/applications?status=interviewed
GET /api/v1/job-publisher/applications?jobId=job123
```

### جلب تفاصيل طلب
```http
GET /api/v1/job-publisher/applications/:applicationId
Authorization: Bearer {token}
```

### تغيير حالة الطلب
```http
PUT /api/v1/job-publisher/applications/:applicationId/status
Authorization: Bearer {token}

{
  "status": "interviewed",
  "message": "رسالة اختيارية للباحث"
}
```

**الحالات المتاحة**:
- `new` - جديد
- `under_review` - قيد المراجعة
- `interviewed` - تمت المقابلة
- `offered` - تم تقديم عرض
- `accepted` - تم القبول
- `rejected` - مرفوض
- `withdrawn` - مسحوب
- `hired` - تم التوظيف

---

## 📍 العنوان الوطني

### الإعداد

1. **الحصول على مفتاح API**:
   - زيارة: https://api.address.gov.sa
   - التسجيل وطلب مفتاح API
   - نسخ المفتاح والـ API ID

2. **إضافة للـ .env**:
```env
NATIONAL_ADDRESS_API_URL=https://api.address.gov.sa/NationalAddress/v3.1/Address/verify
NATIONAL_ADDRESS_API_KEY=your-key-here
NATIONAL_ADDRESS_API_ID=your-id-here
```

### الاختبار
```bash
node test-national-address-api.js
```

**النتيجة المتوقعة**:
```
🧪 Testing National Address API Integration

📋 Environment Configuration:
  API URL: https://api.address.gov.sa/...
  API KEY: ***SET***
  API ID: ***SET***

🏠 Testing: Test Address 1
  📡 Sending request...
  ✅ Verified successfully!
```

### الاستخدام في البروفايل

1. أضف العنوان الوطني في البروفايل
2. اضغط "تحقق من العنوان"
3. النظام يتحقق من العنوان عبر API الحكومة
4. يتم حفظ حالة التحقق

---

## 🌐 التكامل مع الفرونت إند

### مثال: صفحة البروفايل

```tsx
'use client';

import { useState, useEffect } from 'react';
import axios from 'axios';

const JobPublisherProfile = () => {
  const [profile, setProfile] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchProfile();
  }, []);

  const fetchProfile = async () => {
    try {
      const response = await axios.get('/api/v1/job-publisher/profile', {
        headers: {
          Authorization: `Bearer ${localStorage.getItem('token')}`
        }
      });
      setProfile(response.data.profile);
    } catch (error) {
      console.error(error);
    } finally {
      setLoading(false);
    }
  };

  const uploadLogo = async (file: File) => {
    const formData = new FormData();
    formData.append('logo', file);

    try {
      const response = await axios.post(
        '/api/v1/job-publisher/profile/upload-logo',
        formData,
        {
          headers: {
            Authorization: `Bearer ${localStorage.getItem('token')}`,
            'Content-Type': 'multipart/form-data'
          }
        }
      );
      
      toast.success('تم رفع الشعار بنجاح');
      fetchProfile(); // إعادة جلب البيانات
    } catch (error) {
      toast.error('فشل رفع الشعار');
    }
  };

  const verifyNationalAddress = async () => {
    try {
      const response = await axios.post(
        '/api/v1/job-publisher/profile/verify-national-address',
        {},
        {
          headers: {
            Authorization: `Bearer ${localStorage.getItem('token')}`
          }
        }
      );

      if (response.data.verified) {
        toast.success('تم التحقق من العنوان بنجاح');
      } else {
        toast.error('فشل التحقق من العنوان');
      }
      
      fetchProfile();
    } catch (error) {
      toast.error('حدث خطأ أثناء التحقق');
    }
  };

  if (loading) return <div>جاري التحميل...</div>;

  return (
    <div className="container mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">بروفايل الشركة</h1>
      
      {/* قسم الشعار */}
      <div className="mb-8">
        <h2 className="text-xl mb-4">شعار الشركة</h2>
        {profile.companyLogo && (
          <img src={profile.companyLogo} alt="Logo" className="w-32 h-32" />
        )}
        <input
          type="file"
          accept="image/*"
          onChange={(e) => uploadLogo(e.target.files[0])}
        />
      </div>

      {/* قسم العنوان الوطني */}
      <div className="mb-8">
        <h2 className="text-xl mb-4">العنوان الوطني</h2>
        <div className="grid grid-cols-2 gap-4">
          <div>
            <label>رقم المبنى</label>
            <input value={profile.nationalAddress?.buildingNumber || ''} />
          </div>
          <div>
            <label>الرقم الإضافي</label>
            <input value={profile.nationalAddress?.additionalNumber || ''} />
          </div>
          <div>
            <label>الرمز البريدي</label>
            <input value={profile.nationalAddress?.zipCode || ''} />
          </div>
          <div>
            <label>المدينة</label>
            <input value={profile.nationalAddress?.city || ''} />
          </div>
        </div>
        
        {profile.nationalAddress?.verified ? (
          <div className="text-green-600 mt-4">
            ✅ تم التحقق من العنوان
          </div>
        ) : (
          <button
            onClick={verifyNationalAddress}
            className="mt-4 bg-blue-600 text-white px-4 py-2"
          >
            التحقق من العنوان
          </button>
        )}
      </div>
    </div>
  );
};

export default JobPublisherProfile;
```

---

## 🎯 الخلاصة

النظام الآن جاهز 100% من ناحية الباك إند مع:
- ✅ بروفايل شامل ومحترف
- ✅ رفع الصور والمستندات
- ✅ العنوان الوطني + التحقق
- ✅ نظام محادثات متطور
- ✅ نظام إشعارات ذكي
- ✅ إدارة كاملة للوظائف
- ✅ إدارة شاملة للطلبات
- ✅ إحصائيات دقيقة

**لا توجد بيانات وهمية** - كل شيء يعمل فعلياً!

🚀 ابدأ التطوير الآن!
